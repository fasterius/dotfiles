-- Finds the (Neo)Vim version a command was introduced in, useful for finding
-- the required (Neo)Vim version for writing plugins
-- No lazy loading; used to be lazily loaded on commands
vim.pack.add({
    "https://github.com/tweekmonster/helpful.vim",
})
